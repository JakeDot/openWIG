apply plugin: 'java'
apply plugin: 'maven'

group = "com.github.matejcik"

jar.baseName = "openwig"
sourceCompatibility = 8
targetCompatibility = 25
sourceSets {
    main {
        java {
            srcDir 'src'
        }
        resources {
            srcDir 'src'
        }
    }
}
